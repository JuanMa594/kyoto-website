# `content`

Textos largos por sección, cuando los haya. Ahora mismo todo el texto vive en
`src/messages/{es,en}.json`, que es suficiente para cadenas cortas de UI.

Decisión pendiente (§11 del PLAN): si el contenido acaba siendo artículos largos
en vez de tarjetas, esta carpeta pasa a MDX y `messages/` se queda sólo con la
UI.
